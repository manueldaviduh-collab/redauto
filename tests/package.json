{
  "name": "redauto-e2e",
  "private": true,
  "type": "module",
  "description": "Suite de humo Playwright del camino crítico de RedAuto (ver README.md de esta carpeta y docs/ROADMAP.md).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
